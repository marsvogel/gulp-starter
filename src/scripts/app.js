/**
 * Require modules like so:
 * var yourFancyModule = require("./modules/{yourFancyModule}");
 */

var jQuery = require("jquery");

;(function ( $, window, document, undefined ) {
    "use strict";

    /**
     * Your Bootstrap here
     */

})( jQuery, window, document );